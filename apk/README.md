IdeaNotes APK downloads

- Source commit: `786d2e3` on `master`
- App version: `1.0.3+4`
- Build target: Android release APKs (split by ABI)
- Primary file for most phones: `IdeaNotes-1.0.3-786d2e3-arm64-v8a.apk`
- 32-bit fallback: `IdeaNotes-1.0.3-786d2e3-armeabi-v7a.apk`
- SHA-256: see matching `.sha256` files

This build includes:
- Search results keep the active keyword after reload and refresh
- Mobile search switches into an aggressive result-first layout once a query exists
- When the keyboard is up on a phone, the result list keeps visible space instead of being squeezed out of view

Note:
- GitHub does not accept the universal APK because it exceeds the 100 MB file limit, so this branch publishes split APKs instead.
