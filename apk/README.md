IdeaNotes APK downloads

- Source commit: `2c3b9a4` on `master`
- App version: `1.0.3+4`
- Build target: Android release APKs (split by ABI)
- Primary file for most phones: `IdeaNotes-1.0.3-2c3b9a4-arm64-v8a.apk`
- 32-bit fallback: `IdeaNotes-1.0.3-2c3b9a4-armeabi-v7a.apk`
- SHA-256: see matching `.sha256` files

This build includes:
- Search results keep the active keyword after reload and refresh
- Mobile search compresses the oversized top area once a query exists
- Phone-sized screens surface the results list immediately instead of pushing it below the fold

Note:
- GitHub does not accept the universal APK because it exceeds the 100 MB file limit, so this branch publishes split APKs instead.
